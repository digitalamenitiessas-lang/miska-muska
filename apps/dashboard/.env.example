# Base de la API del bot.
#
# En desarrollo se deja VACÍA: el proxy de Vite manda /api a localhost:3001.
# En Vercel hay que definirla, porque el panel es estático y el bot vive en otro
# dominio: Project Settings → Environment Variables.
VITE_API_URL=
